# Release signing template (P13.4). Copy to keystore.properties and fill in.
# keystore.properties + *.jks are gitignored — never commit real secrets.
storeFile=keystore/scrappy-release.jks
storePassword=CHANGE_ME
keyAlias=scrappy
keyPassword=CHANGE_ME